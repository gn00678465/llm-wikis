# Release Notes (synthetic fixture page)

This page's filename stem, `Release_Notes`, is deliberately not slug-shaped
(contains uppercase letters and an underscore), so it can never be a valid
page-provenance citation target under the `[a-z0-9-]+` grammar (spec §11.1)
even though it is a perfectly ordinary Markdown page on disk.
