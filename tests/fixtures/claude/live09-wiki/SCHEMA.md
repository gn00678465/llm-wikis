# Fixture Wiki Schema

This is a synthetic fixture wiki used only to verify `llm-wikis` Task 15's
LIVE-09 row (a Claude local-plugin entrypoint resolving live). It contains
no real content from any registered knowledge base.

- Wiki root: this directory.
- Pages: `overview.md`, `topic-a.md`.
- Link style: `[[slug]]` bare wikilinks, matching `[a-z0-9-]+`.
