# Cargo build output (unanchored: catches the root crate's target/, plus
# spikes/target/, spikes/layout-agnostic/target/, and
# tests/fixtures/process-helper/target/ -- every Cargo crate in this repo)
target/

# Local probe cache copied in for debugging (see docs/2026-07-28-llm-wikis-external-query-design.md §15.1)
probes-v1.json

# Retired local issue tracker — read-only historical archive; new work is
# tracked in .trestle/tasks/ (see AGENTS.md "Historical archives").
.scratch/

# Local Claude Code tooling state (personal plugin toggles, scheduler lock
# file) — machine-local, not shared project configuration.
.claude/
