# llm-wikis

## Agent skills

### Issue tracker

Issues live as markdown files under `.scratch/`; no remote tracker yet (GitHub migration planned). See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary, label strings equal to role names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
